{
  "name": "decklab",
  "private": true,
  "description": "마블 챔피언스 한글 덱 빌더",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "node tools/check-cards.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
